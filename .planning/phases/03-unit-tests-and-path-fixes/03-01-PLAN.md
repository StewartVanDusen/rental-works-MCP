---
phase: 03-unit-tests-and-path-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/storefront.ts
  - src/__tests__/unit/swagger-spec.test.ts
  - src/__tests__/unit/request-bodies.test.ts
autonomous: true
requirements: [PATH-03, PATH-04, PATH-05, PATH-06, TEST-05]
must_haves:
  truths:
    - "storefront_browse_categories calls GET /api/v1/storefront/catalog/{catalogId}/categorytree"
    - "storefront_browse_categories requires a catalogId parameter"
    - "swagger-spec.test.ts passes with zero mismatches after storefront fix"
    - "All 7 it() blocks in request-bodies.test.ts assert capturedUrl and capturedMethod"
    - "checkout/checkin and invoice lifecycle paths confirmed correct via passing swagger-spec.test.ts"
  artifacts:
    - path: "src/tools/storefront.ts"
      provides: "Fixed storefront_browse_categories tool"
      contains: "storefront/catalog/"
    - path: "src/__tests__/unit/swagger-spec.test.ts"
      provides: "Updated storefront test with catalogId arg"
      contains: "catalogId"
    - path: "src/__tests__/unit/request-bodies.test.ts"
      provides: "All it() blocks with capturedUrl assertions"
      contains: "capturedUrl"
  key_links:
    - from: "src/tools/storefront.ts"
      to: "/api/v1/storefront/catalog/{catalogId}/categorytree"
      via: "client.get() call"
      pattern: "client\\.get.*storefront/catalog/"
---

<objective>
Fix the one confirmed path bug (storefront_browse_categories) and audit all existing it() blocks so every assertion includes capturedUrl + capturedMethod. Also verify PATH-05 and PATH-06 are already correct (they are — swagger-spec.test.ts passes).

Purpose: Eliminate all known path mismatches and ensure no shape-only test assertions remain.
Output: Fixed storefront.ts, updated swagger-spec.test.ts, audited request-bodies.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unit-tests-and-path-fixes/03-RESEARCH.md
@src/tools/storefront.ts
@src/__tests__/unit/swagger-spec.test.ts
@src/__tests__/unit/request-bodies.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix storefront_browse_categories path and update swagger-spec test</name>
  <files>src/tools/storefront.ts, src/__tests__/unit/swagger-spec.test.ts</files>
  <read_first>
    src/tools/storefront.ts
    src/__tests__/unit/swagger-spec.test.ts
    scripts/swagger-cache.json
  </read_first>
  <action>
In `src/tools/storefront.ts`, replace the `storefront_browse_categories` tool registration (lines ~49-65) with:

1. Change the tool description to: "Browse the category tree for a storefront catalog. Returns the hierarchical categories customers see."
2. Replace the schema from `{ page: z.coerce.number()..., pageSize: z.coerce.number()... }` to `{ catalogId: z.string().describe("The storefront catalog ID") }`
3. Replace the handler: change from `client.post("/api/v1/storefrontcatalog/browse", { pageno: page, pagesize: pageSize })` to `client.get(\`/api/v1/storefront/catalog/${catalogId}/categorytree\`)`
4. The handler should destructure `{ catalogId }` from args
5. Keep the `withErrorHandling` wrapper
6. Return format stays the same: `{ content: [{ type: "text", text: JSON.stringify(data, null, 2) }] }`

In `src/__tests__/unit/swagger-spec.test.ts`, find the storefront_browse_categories test (around line 716-718):
- Change the it() label from `"storefront_browse_categories → POST /api/v1/storefrontcatalog/browse"` to `"storefront_browse_categories → GET /api/v1/storefront/catalog/{id}/categorytree"`
- Change the callTool args from `{}` to `{ catalogId: "CAT1" }`
- The urlExistsInSpec assertion stays the same (it dynamically checks the captured URL)
  </action>
  <verify>
    <automated>cd "/Users/josh/Coworking Projects/Modern Lighting/Rental Works API" && npx vitest run src/__tests__/unit/swagger-spec.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/tools/storefront.ts contains `catalogId: z.string()` in the storefront_browse_categories schema
    - src/tools/storefront.ts contains `client.get(\`/api/v1/storefront/catalog/${catalogId}/categorytree\`)`
    - src/tools/storefront.ts does NOT contain `storefrontcatalog/browse`
    - src/__tests__/unit/swagger-spec.test.ts contains `catalogId: "CAT1"` in the storefront_browse_categories test
    - swagger-spec.test.ts passes with 0 failures
  </acceptance_criteria>
  <done>storefront_browse_categories calls the correct spec endpoint. swagger-spec.test.ts passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add capturedUrl assertions to all 7 request-bodies.test.ts it() blocks</name>
  <files>src/__tests__/unit/request-bodies.test.ts</files>
  <read_first>
    src/__tests__/unit/request-bodies.test.ts
    src/__tests__/unit/api-paths.test.ts
  </read_first>
  <action>
In `src/__tests__/unit/request-bodies.test.ts`:

1. Add `capturedUrl` to the module-level variables (line 12 area): add `let capturedUrl: string;`

2. In the `beforeEach` block, add `capturedUrl = "";` to the reset section.

3. In the fetch stub, capture the URL: add `capturedUrl = urlStr;` right after `const urlStr = url.toString();` (before the JWT check).

4. Update all 7 it() blocks to add `capturedUrl` and `capturedMethod` assertions. Here are the exact assertions to add to each:

**it("create_invoice_from_order sends { OrderId }"):**
- Add: `expect(capturedMethod).toBe("POST");`
- Add: `expect(capturedUrl).toContain("/api/v1/order/createinvoice");`
- Update it() label to: `"create_invoice_from_order → POST /api/v1/order/createinvoice with { OrderId }"`

**it("apply_order_discount sends { OrderId, DiscountPercent }"):**
- Add: `expect(capturedMethod).toBe("POST");`
- Add: `expect(capturedUrl).toContain("/api/v1/order/discount");`
- Update it() label to: `"apply_order_discount → POST /api/v1/order/discount with { OrderId, DiscountPercent }"`

**it("convert_quote_to_order sends { QuoteId, LocationId, WarehouseId }"):**
- Add: `expect(capturedMethod).toBe("POST");`
- Add: `expect(capturedUrl).toContain("/api/v1/order/convertquote");`
- Update it() label to: `"convert_quote_to_order → POST /api/v1/order/convertquote with { QuoteId, LocationId, WarehouseId }"`

**it("submit_po_for_approval sends { PurchaseOrderId }"):**
- Add: `expect(capturedMethod).toBe("POST");`
- Add: `expect(capturedUrl).toContain("/api/v1/purchaseorder/submitforapproval");`
- Update it() label to: `"submit_po_for_approval → POST /api/v1/purchaseorder/submitforapproval with { PurchaseOrderId }"`

**it("approve_purchase_order sends { PurchaseOrderId }"):**
- Add: `expect(capturedMethod).toBe("POST");`
- Add: `expect(capturedUrl).toContain("/api/v1/purchaseorder/approve");`
- Update it() label to: `"approve_purchase_order → POST /api/v1/purchaseorder/approve with { PurchaseOrderId }"`

**it("reject_purchase_order sends { PurchaseOrderId, RejectReasonNote }"):**
- Add: `expect(capturedMethod).toBe("POST");`
- Add: `expect(capturedUrl).toContain("/api/v1/purchaseorder/reject");`
- Update it() label to: `"reject_purchase_order → POST /api/v1/purchaseorder/reject with { PurchaseOrderId, RejectReasonNote }"`

**it("cancel_order uses POST method (not DELETE)"):**
- Add: `expect(capturedUrl).toContain("/api/v1/order/cancel");`
- Update it() label to: `"cancel_order → POST /api/v1/order/cancel"`

IMPORTANT: Verify the exact paths by reading the source files (`src/tools/orders.ts` and `src/tools/vendors.ts`) BEFORE writing the assertions. The paths listed above are based on research — confirm them against the actual source code. If any path differs from what is listed above, use the path from the source code.
  </action>
  <verify>
    <automated>cd "/Users/josh/Coworking Projects/Modern Lighting/Rental Works API" && npx vitest run src/__tests__/unit/request-bodies.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - request-bodies.test.ts contains `let capturedUrl: string`
    - request-bodies.test.ts contains `capturedUrl = "";` in beforeEach
    - request-bodies.test.ts contains `capturedUrl = urlStr;` in the fetch stub
    - Every it() block in request-bodies.test.ts contains `expect(capturedUrl).toContain(`
    - Every it() block in request-bodies.test.ts contains `expect(capturedMethod).toBe(`
    - `npx vitest run src/__tests__/unit/request-bodies.test.ts` exits with 0 failures
  </acceptance_criteria>
  <done>All 7 it() blocks in request-bodies.test.ts assert capturedUrl, capturedMethod, and capturedBody. No shape-only assertions remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan fixes an endpoint path and adds test assertions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | storefront_browse_categories | accept | Path change only affects which RW API endpoint is called; no auth or data handling change |
</threat_model>

<verification>
1. `npx vitest run src/__tests__/unit/swagger-spec.test.ts --reporter=dot` — zero failures
2. `npx vitest run src/__tests__/unit/request-bodies.test.ts --reporter=dot` — zero failures
3. `npm test -- --reporter=dot` — full suite green
4. `grep -c "capturedUrl" src/__tests__/unit/request-bodies.test.ts` returns >= 8 (1 declaration + 7 assertions)
5. `grep "storefrontcatalog/browse" src/tools/storefront.ts` returns no matches (old path removed)
</verification>

<success_criteria>
- storefront_browse_categories calls GET /api/v1/storefront/catalog/{catalogId}/categorytree
- swagger-spec.test.ts passes with 0 mismatches
- All 7 request-bodies.test.ts it() blocks assert capturedUrl + capturedMethod + capturedBody
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-tests-and-path-fixes/03-01-SUMMARY.md`
</output>
