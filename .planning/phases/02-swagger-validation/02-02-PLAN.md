---
phase: 02-swagger-validation
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/__tests__/unit/swagger-spec.test.ts
autonomous: true
requirements:
  - PATH-01
  - PATH-07

must_haves:
  truths:
    - "Every one of the 114 tools has a test case that validates its API path against the Swagger spec"
    - "The test checks both HTTP method AND URL path — not just one"
    - "Path parameters in captured URLs are normalized to match spec placeholders"
    - "Query strings are stripped before path comparison"
    - "No tool is silently skipped — every tool produces a pass or explicit fail"
    - "The test suite passes green confirming all 114 paths are spec-correct"
  artifacts:
    - path: "src/__tests__/unit/swagger-spec.test.ts"
      provides: "Automated Swagger path validation for all 114 tools"
      min_lines: 200
  key_links:
    - from: "src/__tests__/unit/swagger-spec.test.ts"
      to: "scripts/swagger-cache.json"
      via: "readFileSync + JSON.parse import"
      pattern: "swagger-cache\\.json"
    - from: "src/__tests__/unit/swagger-spec.test.ts"
      to: "src/tools/*.ts"
      via: "register*Tools functions called on MCP server"
      pattern: "register.*Tools"
---

<objective>
Create a comprehensive Vitest test file that validates every one of the 114 MCP tool API paths against the cached Swagger spec, confirming method + path correctness for each tool.

Purpose: This is the primary deliverable of Phase 2 — a machine-readable pass/fail report that proves all existing tool paths match the authoritative Swagger spec. Phase 3 will use any failures as a fix checklist.
Output: src/__tests__/unit/swagger-spec.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-swagger-validation/02-RESEARCH.md
@.planning/phases/02-swagger-validation/02-01-SUMMARY.md

<interfaces>
<!-- From src/__tests__/unit/api-paths.test.ts — the established URL capture test pattern -->
<!-- The swagger-spec test MUST follow this exact same setup pattern -->

Test setup boilerplate:
- Import McpServer, Client, InMemoryTransport from MCP SDK
- Import all 11 register*Tools functions
- Import resetClient from api-client
- Stub global.fetch with vi.stubGlobal to capture URL + method
- JWT mock returning { statuscode: 200, access_token: "test-token", ... }
- Browse mock returning { TotalRows: 0, Rows: [], ... }
- Entity mock returning { Id: "123" }
- callTool(name, args) helper that calls client.callTool

From scripts/swagger-cache.json (generated by Plan 01):
- Structure: { generatedAt, baseUrl, subSpecs, totalPaths, paths: Array<{ method, path, spec }> }
- paths[].method is uppercase (GET, POST, PUT, DELETE)
- paths[].path uses OpenAPI placeholders like {id}, {reportid}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create swagger-spec.test.ts with path validation for all 114 tools</name>
  <files>src/__tests__/unit/swagger-spec.test.ts</files>
  <read_first>
    src/__tests__/unit/api-paths.test.ts
    scripts/swagger-cache.json
    src/tools/inventory.ts
    src/tools/orders.ts
    src/tools/contracts.ts
    src/tools/customers.ts
    src/tools/billing.ts
    src/tools/vendors.ts
    src/tools/settings.ts
    src/tools/reports.ts
    src/tools/admin.ts
    src/tools/storefront.ts
    src/tools/utilities.ts
    src/utils/tool-helpers.ts
  </read_first>
  <action>
Create `src/__tests__/unit/swagger-spec.test.ts` that validates every tool's captured API path + method against the Swagger spec cache.

**Setup (copy pattern from api-paths.test.ts):**
1. Import all 11 `register*Tools` functions (inventory, orders, contracts, customers, billing, vendors, settings, reports, admin, storefront, utilities)
2. Import `resetClient` from `../../utils/api-client.js`
3. Import MCP SDK: `McpServer`, `Client`, `InMemoryTransport`
4. Import vitest: `describe`, `it`, `expect`, `beforeAll`, `afterAll`, `beforeEach`, `vi`
5. Load swagger cache: `import { readFileSync } from "fs"` + `import { dirname, join } from "path"` + `import { fileURLToPath } from "url"`, then:
   ```typescript
   const __dirname = dirname(fileURLToPath(import.meta.url));
   const cache = JSON.parse(readFileSync(join(__dirname, "../../../scripts/swagger-cache.json"), "utf8"));
   const specPaths: Array<{ method: string; path: string; spec: string }> = cache.paths;
   ```

**Path matching utility functions (define before describe block):**
```typescript
/** Convert OpenAPI path template to regex. /api/v1/order/{id} -> /api\/v1\/order\/[^/]+ */
function specPathToRegex(specPath: string): RegExp {
  const escaped = specPath.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const pattern = escaped.replace(/\\\{[^}]+\\\}/g, "[^/]+");
  return new RegExp(`^${pattern}$`);
}

/** Check if captured URL path + method exists in the spec */
function urlExistsInSpec(capturedPath: string, capturedMethod: string): boolean {
  const cleanPath = capturedPath.split("?")[0]; // strip query string
  return specPaths.some(
    entry => entry.method === capturedMethod && specPathToRegex(entry.path).test(cleanPath)
  );
}
```

**Server setup (beforeAll):**
- Register ALL 11 tool domains on a single McpServer instance (the existing api-paths.test.ts only registers 7 — this test must register all 11 including admin, billing, customers, settings)
- Set env vars: `RENTALWORKS_USERNAME`, `RENTALWORKS_PASSWORD`, and `RENTALWORKS_BASE_URL` (set to `"http://test.rentalworks.cloud"` for URL capture)
- Connect via InMemoryTransport

**Fetch stub (beforeEach):**
- Reset client and captured vars
- Stub fetch to capture URL + method
- Return appropriate mock responses:
  - JWT endpoint -> JWT_RESPONSE
  - URLs containing `/browse`, `/runreport`, `/conflicts`, `/render`, `/availabilitylegend` -> BROWSE_RESPONSE (array-like)
  - All other URLs -> ENTITY_RESPONSE (single object)

**Test cases — use a table-driven approach with `it.each` or individual `it()` blocks organized by domain.**

For EACH of the 114 tools, write an `it()` block that:
1. Calls the tool with minimal valid arguments (just enough for the handler to make an API call)
2. Captures the URL via the fetch stub
3. Strips the base URL prefix to get the path
4. Asserts `urlExistsInSpec(path, capturedMethod)` is `true`

Organize by domain with `describe` blocks:

```typescript
describe("swagger spec validation", () => {
  describe("inventory (13 tools)", () => {
    it("browse_rental_inventory", async () => { ... });
    // ...
  });
  describe("orders (15 tools)", () => { ... });
  // ... one describe per domain
});
```

**Critical tool argument requirements (tools that need specific args to trigger an API call):**
- Browse tools: `{}` is sufficient (uses defaults)
- Get-by-ID tools: pass `{ orderId: "X1" }` or `{ InventoryId: "X1" }` etc. (any string works)
- `get_item_by_barcode`: `{ barcode: "BC001" }`
- `cancel_order`: `{ orderId: "O1" }`
- `create_invoice_from_order`: `{ orderId: "O1" }`
- `convert_quote_to_order`: `{ quoteId: "Q1", locationId: "L1", warehouseId: "W1" }`
- `apply_order_discount`: `{ orderId: "O1", discountPercent: 10 }`
- `run_report` and `run_report_data`: `{ reportName: "LateReturnsReport" }`
- `export_report_excel`: `{ reportName: "LateReturnsReport" }`
- `storefront_get_product`: `{ productId: "P1", warehouseId: "W1", locationId: "L1", fromDate: "2025-01-01", toDate: "2025-01-31" }`
- `change_icode`: `{ InventoryId: "I1", NewICode: "NEW" }`
- `assign_barcodes`: `{ InventoryId: "I1" }`
- `ai_assistant`: `{ query: "hello" }`
- `checkout_start_session`: `{ contractId: "C1" }`
- `checkout_stage_item`: needs session + barcode args (read the tool handler to determine)
- `checkin_start_session`: `{ contractId: "C1" }`
- `checkin_item`: needs session + barcode args (read the tool handler)
- `raw_api_browse`: `{ entityName: "order" }`
- `raw_api_get`: `{ entityName: "order", id: "X1" }`
- `raw_api_post`: `{ path: "/api/v1/order/browse", body: {} }`
- `sync_to_quickbooks`: needs appropriate args (read the tool handler)
- `approve_invoice`, `process_invoice`, `void_invoice`: `{ invoiceId: "I1" }`
- `create_billing_estimate`: needs billing args (read the tool handler)
- `browse_settings_entity`: `{ entityName: "warehouse" }`

**You MUST read each tool domain file** to determine the exact parameter names and minimum required arguments. Do not guess — the Zod schemas in each handler define what's required.

**Special handling for tools that don't make API calls or have dynamic paths:**
- `raw_api_browse`, `raw_api_get`, `raw_api_post`: These are generic passthrough tools. Test them with a known entity that maps to a spec path (e.g., `entityName: "order"` -> `/api/v1/order/browse`).
- `run_report`, `run_report_data`, `export_report_excel`: Use `reportName: "LateReturnsReport"` so the dynamic path resolves to `/api/v1/latereturnsreport/runreport` which exists in the spec.
- `browse_settings_entity`: Use `entityName: "warehouse"` so it resolves to `/api/v1/warehouse/browse`.

**After writing all test cases, verify the count:** Add a meta-test at the top or bottom that confirms the total number of tool test cases matches the expected count. Use the MCP client's `listTools()` to get the actual tool count:
```typescript
it("covers all registered tools", async () => {
  const { tools } = await client.listTools();
  // Each tool should have a corresponding test in this file
  expect(tools.length).toBe(114);
});
```
  </action>
  <verify>
    <automated>cd "/Users/josh/Coworking Projects/Modern Lighting/Rental Works API" && npx vitest run --project unit src/__tests__/unit/swagger-spec.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/unit/swagger-spec.test.ts exists
    - File imports all 11 register*Tools functions (grep for "registerInventoryTools", "registerOrderTools", "registerContractTools", "registerCustomerTools", "registerBillingTools", "registerVendorTools", "registerSettingsTools", "registerReportTools", "registerAdminTools", "registerStorefrontTools", "registerUtilityTools")
    - File imports and parses swagger-cache.json (grep for "swagger-cache.json")
    - File contains specPathToRegex function (grep for "specPathToRegex")
    - File contains urlExistsInSpec function (grep for "urlExistsInSpec")
    - File has test cases for all 114 tools (no tool silently skipped)
    - All test cases pass when run via vitest
    - Existing api-paths.test.ts tests still pass
  </acceptance_criteria>
  <done>swagger-spec.test.ts passes for all 114 tools, confirming every path+method matches the Swagger spec. No tool is silently skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite and document results</name>
  <files>src/__tests__/unit/swagger-spec.test.ts</files>
  <read_first>
    src/__tests__/unit/swagger-spec.test.ts
  </read_first>
  <action>
Run the complete unit test suite to confirm no regressions:

```bash
npx vitest run --project unit
```

Verify:
1. All existing tests in api-paths.test.ts, removed-tools.test.ts, request-bodies.test.ts, tool-helpers.test.ts, tool-registration.test.ts still pass
2. The new swagger-spec.test.ts passes all test cases
3. No test is skipped or pending

If any swagger-spec test cases fail:
- Investigate the failure: is it a test argument issue (wrong param name) or an actual path mismatch?
- Fix test arguments if the failure is due to incorrect test setup
- If a genuine path mismatch is found, the test should STILL report it as a failure (this is the desired behavior — Phase 3 will fix path bugs). Document any mismatches in the SUMMARY.

Ensure the test output clearly shows pass/fail per tool so it serves as the "confirmed list of path bugs" required by the phase success criteria.
  </action>
  <verify>
    <automated>cd "/Users/josh/Coworking Projects/Modern Lighting/Rental Works API" && npx vitest run --project unit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project unit` exits with code 0
    - All 6 test files pass (5 existing + 1 new swagger-spec)
    - swagger-spec.test.ts reports results for all 114 tools
    - No test is silently skipped
  </acceptance_criteria>
  <done>Full unit test suite passes. The swagger-spec test validates all 114 tool paths against the spec. Any path mismatches are documented for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file to cached spec | Test trusts swagger-cache.json as authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T (Tampering) | swagger-cache.json consumed by test | accept | Cache is committed to git; any tampering would show in git diff. Cache is regenerated from live spec via documented script. |
| T-02-04 | R (Repudiation) | Test results | accept | Vitest output provides timestamped pass/fail evidence. CI integration in future phases will provide audit trail. |
</threat_model>

<verification>
1. `npx vitest run --project unit` passes with 6 test files (5 existing + swagger-spec)
2. swagger-spec.test.ts contains test cases for all 114 tools
3. Every test case checks both method AND path
4. Path normalization handles {id} placeholders and query strings
5. No tool is silently omitted from validation
</verification>

<success_criteria>
- swagger-spec.test.ts validates all 114 tools against the cached Swagger spec
- All tests pass (confirming all paths are correct per research findings)
- The test can be re-run after cache regeneration to catch future regressions
- Phase 3 can use any test failures as a fix checklist
</success_criteria>

<output>
After completion, create `.planning/phases/02-swagger-validation/02-02-SUMMARY.md`
</output>
